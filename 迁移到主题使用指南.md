# Eva Framework · 迁移到主题使用指南

> 目标读者：把 Eva（CSF 替代框架）从「独立插件」搬进 **Lentasy 主题** 内嵌使用、并逐步替换现有 CSF 配置的人。
> 版本基准：Eva `0.1.0`。本文所有结论均来自对源码的核对（`eva-framework.php` / `includes/` 下各 class，按 core/admin/containers/demo 分目录）。

---

## 0. 一句话结论（TL;DR）

- **demo 可删**：`includes/demo-options.php` 及 `eva-framework.php` 末尾对它的 `require` 属于演示，删掉不影响框架本体。
- **能搬进主题**：Eva 幂等加载（`EVA_FW_LOADED`），可像 CSF 那样被主题 `require` 内嵌。
- **迁移本质**：① 主题里 require Eva 本体 → ② `\CSF::createXxx` 改成 `\Eva::createXxx` → ③ **前台取值层基本不用动**（同样存 `wp_options` / `post_meta` / `term_meta`，键名沿用容器 id）。
- **但别急着全量迁**：当前字段类型 / 后端 sanitize 不全（详见 §6），需**边补边迁**。

---

## 1. 现状与前置认知

| 项 | 说明 |
| --- | --- |
| 位置 | `wp-content/plugins/Eva framework/` |
| 入口 | `eva-framework.php` |
| 命名空间 | 渲染/容器类在 `Eva\Framework\*`；对外注册门面是**全局类** `\Eva` |
| 幂等加载 | 定义 `EVA_FW_LOADED` 常量，重复 `require` 自动 `return`，可安全内嵌主题 |
| 数据存储 | 设置页 → `wp_options[$id]`；metabox → `post_meta`；分类法 → `term_meta`；导航菜单 → 菜单项 `post_meta`（与 CSF 一致） |
| 资源 | Vue3 + Remixicon 走 jsDelivr CDN；`assets/eva-app.js` 为前端外壳；`Fields/<name>/<name>.js` 一字段一目录 |

---

## 2. 把 Eva 搬进主题（两种方式）

### 方式 A · 保留为插件（最省事，建议迁移期先用）

不搬代码，启用插件后 `\Eva` 类已全局可用，主题任意位置直接调用 `\Eva::createOptions()` 即可。
- 优点：零改造，CSF 与 Eva 可并存，逐模块切换。
- 缺点：依赖插件「已启用」状态，不适合对外发布的主题。

### 方式 B · 内嵌进主题（与 CSF 同款，发布主题用）

1. 复制框架本体到主题，例如：
   `wp-content/themes/Lentasy/Library/eva-framework/`（保留 `includes/` 与 `assets/`）。

2. 在 `loader.php` 的 `load_library()` 中，仿照现有 CSF 那一行加载（迁移期两者共存）：

```php
//加载 Codestar 框架（迁移完成后可移除）
require_once LF_THEME_DIR . LF_DS . 'Library' . LF_DS . 'codestar-framework' . LF_DS . 'codestar-framework.php';

//加载 Eva 框架
require_once LF_THEME_DIR . LF_DS . 'Library' . LF_DS . 'eva-framework' . LF_DS . 'eva-framework.php';
```

3. 内嵌时注意 `eva-framework.php` 里的三处：
   - `register_activation_hook(__FILE__, ...)`：主题内嵌没有插件激活钩子，**不会触发**，无害；伪静态 flush 由 `Standalone` 的「规则签名」机制兜底。
   - `EVA_FW_DEV`：**生产务必关闭**（见 §8）。
   - demo 的 `require`：内嵌前请按 §7 删除。

> 提示：`EVA_FW_DIR` / `EVA_FW_URL` 用 `plugin_dir_path/url(__FILE__)` 推导，放在主题子目录里同样成立，无需改路径常量。

---

## 3. 注册 API 对照表（CSF → Eva）

API **同名同签**，把前缀 `\CSF::` 换成 `\Eva::` 即可：

| 容器 | CSF | Eva | 存储位置 |
| --- | --- | --- | --- |
| 设置页 | `CSF::createOptions` | `Eva::createOptions` | `wp_options[$id]` |
| 分组 | `CSF::createSection` | `Eva::createSection` | （归属所在容器） |
| 文章 metabox | `CSF::createMetabox` | `Eva::createMetabox` | `post_meta` |
| 分类法 | `CSF::createTaxonomyOptions` | `Eva::createTaxonomyOptions` | `term_meta` |
| 导航菜单 | `CSF::createNavMenuOptions` | `Eva::createNavMenuOptions` | 菜单项 `post_meta` |
| 用户资料 | `CSF::createProfileOptions` | `Eva::createProfileOptions` | `user_meta` |
| 评论 | `CSF::createCommentMetabox` | `Eva::createCommentMetabox` | `comment_meta` |
| 定制器 | `CSF::createCustomizeOptions` | `Eva::createCustomizeOptions` | `wp_options[$id]` |
| 短代码 | `CSF::createShortcoder` | `Eva::createShortcoder` | — |
| 小工具 | `CSF::createWidget` | `Eva::createWidget` | widget 实例 |
| 区块编辑器区块 | —（CSF 无） | `Eva::createBlock` | 文章内容里的区块属性 |

### 品牌主色

Eva 默认是粉色（`#ff758c`）。主题换成自己的品牌色只需一行，**不要去改 `eva.css`**：

```php
\Eva::setThemeColor('#3858e9');   // 十六进制，#rgb / #rrggbb 都行
```

也可以用过滤器动态决定：`add_filter('eva_theme_color', fn() => get_theme_mod('accent'))`。

主色令牌集中在 `assets/eva.css` 的 `:root`（`--eva-primary` / `-600` / `-050` / `--eva-on-primary`），设了之后 PHP 会在 eva.css 之后追加一段同名令牌覆盖它，**深浅两档由 `color-mix` 从这一个值推导**，调用方不用分别指定。设置页、独立页、嵌入式字段、metabox 面板的强调条、TinyMCE 工具栏上的短代码按钮、保存条、前台的后台悬浮窗等所有 Eva 组件会一起跟着变（悬浮窗跑在没有 eva.css 的前台，令牌由它自己的 `#xn-fa-root` 那段就近输出）。

**优先级：用户在设置抽屉「主题色」里挑的那档 > 主题设的品牌色 > eva.css 的默认粉。** 抽屉的选择存在当前用户的 `user_meta`（`eva_fw_accent`，一份含 key 与三档色值的数组），只影响他自己，不会动主题的设定；用户没挑过时抽屉里高亮的就是品牌色对应的那档，品牌色不在 8 档预设里则一档都不高亮。暗色模式同理存 `user_meta`（`eva_fw_dark`），它优先于 `createOptions` 的 `theme` 参数。两者都由 `wp_ajax_eva_fw_set_accent` / `wp_ajax_eva_fw_set_dark` 保存，属个人偏好，不要求 `manage_options`。

颜色会被拼进 CSS，所以只接受十六进制并过 `sanitize_hex_color()`；传了非法值会返回 `false` 并保持原色。中性色（背景 / 边框 / 文字）仍由各组件自带，因为它们要配合暗色模式 `.eva-dark`，不受这个开关影响。

### 区块编辑器区块（`createBlock`，Eva 独有）

CSF 没有对应 API——它所谓的「区块」只是 `createShortcoder` 附带注册的一个短代码插入器，
区块本体只有一个文本框。Eva 的 `createBlock` 是真正的区块：字段在编辑器右侧栏收，
前台由 PHP 渲染（动态区块）。

```php
\Eva::createBlock('eva/notice', [
    'title'    => '提示框',
    'icon'     => 'info',              // dashicon 名，或整段 SVG
    'category' => 'lentasy',           // 分类不存在时自动注册
    'category_title' => '柠檬主题',     // 自动注册时的显示名
    'fields'   => [                    // 只有一组字段时可省掉 sections 这层
        ['id' => 'style', 'type' => 'button_set', 'title' => '样式',
         'options' => ['info' => '信息', 'warning' => '警告'], 'default' => 'info'],
        ['id' => 'text',  'type' => 'textarea',   'title' => '内容'],
    ],
    'render'   => 'lf_render_notice',  // callable($values, $content, $block)
]);

function lf_render_notice($values, $content = '', $block = null) {
    return '<div class="lf-notice is-' . esc_attr($values['style']) . '">'
        . wp_kses_post(wpautop($values['text'])) . '</div>';
}
```

不写 `render` 时回退到过滤器 `eva_block_{区块名}`（同短代码容器的 `eva_shortcode_{tag}` 套路）：

```php
add_filter('eva_block_eva/notice', function ($html, $values, $content, $block) { … }, 10, 4);
```

**参数**

| 参数 | 默认 | 说明 |
| --- | --- | --- |
| `title` / `description` / `icon` / `keywords` | — | 可写成 `{zh,en,ja}` 多语言对象 |
| `category` / `category_title` | `widgets` | 分类未注册时自动补一条 |
| `render` | — | `callable($values, $content, $block)`；没有则走 `eva_block_{name}` |
| `preview` | 跟随 `render` 是否可用 | 画布内显示服务端渲染的真实效果 |
| `placement` | `inspector` | `inspector`=字段在右侧栏；`content`=字段直接画在区块里 |
| `inner_blocks` | `false` | 支持嵌套子区块；此时画布留给 InnerBlocks，不做预览 |
| `supports` / `example` | — | 原样透传 `register_block_type`；`example` 也接受直接写字段值的简写 |
| `capability` | `edit_posts` | 权限不足的用户在插入器里看不到它（区块本身仍注册，不影响已有内容渲染） |
| `fields` / `sections` | — | `fields` 是单分区简写；也可以照常用 `Eva::createSection('eva/notice', […])` |

**把字段放进区块工具栏**

字段上标 `'toolbar' => true`，它就从右侧栏搬到选中区块时浮出来的那条工具栏上（不会两边都出现）。
只有离散选项类的字段塞得进去——`button_set`、`radio`、`select`、`switcher`，其余类型忽略这个标记、留在侧栏。

```php
['id' => 'align', 'type' => 'button_set', 'title' => '对齐',
 'toolbar' => true,
 // 选项写成 ['label' => …, 'icon' => …] 时只显示图标，文字进 tooltip
 'options' => [
     'left'   => ['label' => '左',   'icon' => 'editor-alignleft'],
     'center' => ['label' => '居中', 'icon' => 'editor-aligncenter'],
 ]],

// 开关在工具栏上是一个按下态的图标按钮
['id' => 'rounded', 'type' => 'switcher', 'title' => '圆角',
 'toolbar' => true, 'toolbar_icon' => 'align-center'],
```

工具栏控件用的是 WordPress 原生的 `ToolbarButton` / `ToolbarDropdownMenu`，不是 Eva 的 Vue 组件——
一排紧凑按钮里塞 Vue 应用既重又不像原生。两边读写的是同一个区块属性，值天然同步。

**几点要知道的**

- 区块名必须是 `namespace/name`；只给了 name 会自动补 `eva/` 前缀。
- **值不进数据库**，而是作为一个对象属性 `eva` 写在文章内容里：
  `<!-- wp:eva/notice {"eva":{"style":"warning",…}} /-->`。
  渲染回调拿到的 `$values` 是拍平的 `[field_id => value]`，已经补过默认值、过过字段清洗，
  形态和其它容器一致（开了 `csf_compat` 就是 CSF 形态），所以 `_LF_*` 那套读法照用。
  之所以不逐字段一个属性：区块属性带类型校验，Eva 四十来种字段值形态从标量到嵌套数组都有，
  逐字段映射既易错，作者改字段类型时还会让文章里已有的区块失效。
- 全部是动态区块（`save` 返回 `null`），停用 Eva 后文章里留下的是空区块而不是坏掉的 HTML。
- `placement => 'content'` 时字段画在画布 iframe 里，Eva 会自动把样式表注入画布；
  站点没有这类区块时一个字节都不注入。

### 文章 metabox：侧栏与面板皮肤

`createMetabox` 的 `post_type` / `context` / `priority` / `data_type` / `page_templates` / `post_formats` / `exclude_post_types` 都与 CSF 同名同义，`'data_type' => 'unserialize'` 原样照抄即可（Eva 内部映射成 `direct`，落库仍是逐字段独立 `post_meta`，`_LF_*` 取值函数不用改）。

两个 Eva 独有的参数 / 开关：

| 写法 | 默认 | 作用 |
| --- | --- | --- |
| `'block_editor_compatible' => false` | `true` | 关掉后该 metabox 在区块编辑器里退回页面底部的兼容面板。默认已声明为兼容，正常待在侧栏，不需要写这行；只有字段依赖经典编辑器 DOM 时才关。 |
| `\Eva::skinAdminBoxes(true)` | `false` | 面板皮肤铺满整条文章编辑页侧栏。 |

`context => 'side'` 不需要额外配置：侧栏可用宽度只有 230–260px，Eva 会自动切到窄栏变体（去掉表单外框避免卡中卡、内边距收紧、栅格强制单列）。

**关于 `skinAdminBoxes`**：保持默认的 `false`。WordPress 原生 postbox 是直角 + 深灰边框 + 标题栏硬分割线，和 Eva 的圆角浅色控件是两套语言，所以 Eva 把自己那些装着字段的面板的外壳接管过来；但「发布」「分类目录」「特色图片」这些官方面板、以及别的插件的面板，一律保持原生外观——**框架的样式不该外溢到不属于它的 UI 上**。打开这个开关会把皮肤铺满整条侧栏，代价就是官方面板跟着变样，**Lentasy 不用它**。也可以用过滤器 `eva_skin_admin_boxes` 动态决定。

### 用户资料字段写法（CSF Profile Options）

用户资料容器挂在 WordPress 的 `profile.php` / `user-edit.php`，字段值保存在
`user_meta`，不应使用 `get_option()` 读取。Eva 与 CSF 保持相同的注册思路：

```php
$prefix = 'my_profile_options';

Eva::createProfileOptions($prefix, [
    'data_type' => 'serialize', // 一条 user_meta，值为 [field_id => value]
]);

Eva::createSection($prefix, [
    'title'  => '用户偏好',
    'fields' => [
        [
            'id'      => 'email_notice',
            'type'    => 'switcher',
            'title'   => '邮件通知',
            'default' => 1,
        ],
        [
            'id'      => 'language',
            'type'    => 'select',
            'title'   => '偏好语言',
            'options' => ['zh' => '简体中文', 'en' => 'English'],
            'default' => 'zh',
        ],
    ],
]);
```

读取值：

```php
$profile = get_user_meta($user_id, 'my_profile_options', true);
$enabled = ! empty($profile['email_notice']);
```

将 `data_type` 改为 `direct` 时，每个字段会以自己的字段 ID 独立写入
`user_meta`。保存过程自动挂接 `personal_options_update` 和
`edit_user_profile_update`，并执行字段级清洗与 nonce 校验。

### `createOptions` 参数差异（重点）

CSF 专有键 → 迁移时**删除或改写**；Eva 新增了独立页能力：

```php
// CSF 写法（主题现状，Modules/Settings/Main.php）
\CSF::createOptions('Leantasy', array(
    'framework_title' => '...',          // ✗ Eva 无，删
    'menu_title'      => 'LF主题设置',     // ✓ 保留
    'menu_slug'       => 'Leantasy',     // ✓ 保留
    'menu_icon'       => 'https://...',  // ✓ 保留
    'class'           => '...',          // ✗ 删
    'menu_type'       => 'menu',         // ✗ 删（用 location 代替）
    'show_bar_menu'   => false,          // ✗ 删
    'theme'           => 'light',        // ✗ 删
));

// Eva 写法
\Eva::createOptions('Leantasy', array(
    'menu_title'  => 'LF主题设置',
    'menu_slug'   => 'Leantasy',
    'menu_icon'   => 'dashicons-admin-customizer', // 或图片 URL
    'location'    => 'admin_bar',  // admin_bar=顶部工具栏入口；left=左侧菜单
    'standalone'  => true,         // true=独立页(脱离 /wp-admin/)；false=后台全屏页
    'brand'       => 'Leantasy',   // 侧栏品牌名
    'subtitle'    => '...',
    'menu'        => array(),       // 左侧主菜单项；也可用 \Eva::addMenuItem() 追加
));
```

---

## 4. 接入主题的三段式加载

主题 `loader.php → load_modules()` 现有三阶段，迁移时**流程不变**，只把内部 `\CSF::` 改 `\Eva::`：

1. **阶段一** `init_framework()`：`\Eva::createOptions(...)` 建容器。
2. **阶段二** 加载扩展模块：扩展通过 `do_action('Extend_Settings')` 等钩子注册分组。
3. **阶段三** `init_sections()`：各模块 `\Eva::createSection(...)` 注册字段；`\Eva::createTaxonomyOptions(...)` 建分类法容器。

> 容器 id 不要与分类法 id 同名（主题已用 `Leantasy` 与 `Leantasy_Taxonomy` 区分），Eva 同样按 id 索引，沿用即可。

---

## 5. 字段写法与取值

### 字段数组

```php
\Eva::createSection('Leantasy', array(
    'id'     => 'general',
    'title'  => '常规设置',
    'icon'   => 'ri-equalizer-line',
    'fields' => array(
        array('id' => 'site_slogan', 'type' => 'text',     'title' => '站点标语', 'desc' => '...', 'default' => ''),
        array('id' => 'enable_x',    'type' => 'switcher',  'title' => '开关',     'default' => false),
        array('id' => 'mode',        'type' => 'select',    'title' => '模式',     'options' => array('a' => 'A', 'b' => 'B')),
    ),
));
```

- **键名差异**：CSF 的 `subtitle`（标题下小字）≈ Eva 的 `desc`；`width` 支持 `1/2`、`1/3`、`full`。

### 取值（迁移友好点）

Eva 与 CSF **同样把设置页存进 `wp_options`，键 = 容器 id，值 = `[field_id => value]`**。
因此主题现有读取层 **无需改动**：

```php
// 主题现成 helper（Modules/Settings/Main.php），内部就是 get_option('Leantasy')[$key]
\LF\Modules\Settings\Main::get_option('site_slogan');
\LF\Modules\Settings\Main::get_term($term_id, 'key');   // term_meta
\LF\Modules\Settings\Main::get_met_box($post_id, 'key', 'slug'); // post_meta
\LF\Modules\Settings\Main::get_nav($item_id, 'key');    // 菜单项 post_meta
```

框架自带读取（等价）：

```php
\Eva::get_values('Leantasy');     // 取整组数组
get_option('Leantasy');           // 原始
```

> 结论：**前台 / 模板层基本零改动**，迁移主要发生在「注册端」。

---

## 6. 当前差距与迁移顺序（务必先读）

> ⚠️ 本节写于 0.1.0，下面列的「字段组件不全 / sanitize 不全」已经补齐，保留仅供回顾。
> CSF 写法现在怎么兼容、哪些还不兼容，以 **§10 CSF 兼容层** 为准。

> 这是 0.1.0 的真实短板，**不读会丢数据**。

1. **后端 sanitize 不全**：`Data::sanitize_field()` 目前只认 `text / textarea / select / switcher`，其余一律走 `default → sanitize_text_field((string)$val)`。
   → `group / repeater / fieldset / tabbed / accordion / gallery / checkbox` 等**值为数组**的字段，存盘会被强转成字符串、**数据损坏**。
   → **迁任何含数组型字段的模块前，先扩 `Data::sanitize_field()`**（至少：数组值递归清洗 / 按类型分支）。
2. **前端组件不全**：`Fields/` 仅 `text / textarea / select / switcher / html / backup` 6 种；注册其它 `type` 前台**渲染不出**。
3. **主题实际用到、Eva 暂缺的约 20 种**：
   - 简单输入：`number / radio / checkbox / button_set / spinner / subheading / content`
   - 媒体颜色：`upload / gallery / image_select / icon / color / color_group / palette`
   - 嵌套容器（最难）：`fieldset / group / repeater / tabbed / accordion / sorter`

### 推荐迁移顺序

1. **试点模块**：挑字段少、类型简单的真实模块（如「常规设置 / Routine」），补齐它需要的字段（组件 + sanitize 分支），整模块迁过去，端到端验证：**注册 → 渲染 → 提交 → 存库 → 回读**。
2. 试点跑通后，按「字段类型出现频率」补：先广覆盖的简单输入类，每补一类就迁用到它的一批分区。
3. 媒体 / 颜色类居中。
4. **嵌套容器放最后**——最复杂，且要等简单字段契约定死后做，风险最低。

---

## 7. demo 删除清单（迁移时务必删）

> **`includes/demo-options.php` 只是一个演示**：它注册了名为 `eva_demo` 的示例设置页（菜单树、几个示例字段、帮助/版本/备份占位），用来展示 Eva 怎么用，**不是主题正式配置**。
> **迁移到主题时应当删除它**，改用主题自己的容器 id（如 `Leantasy`）注册真正的设置；留着它只会多出一个无用的「Eva Framework」演示菜单。

删除步骤：

1. 删文件：`includes/demo-options.php`
2. 删 `eva-framework.php` 末尾这段 `require`（这是唯一加载入口，删了 demo 就彻底不加载）：

```php
// 内置演示设置页（可删除此 require 与 includes/demo-options.php）
require_once EVA_FW_DIR . 'includes/demo-options.php';
```

3. 确认没有别处引用演示容器 id `eva_demo`（迁移到主题后应改用主题真实容器 id，如 `Leantasy`）。
4. 区块演示是单独的一份：删 `includes/demo-blocks.php` 和 `eva-framework.php` 里对它的 `require`。它注册了 `eva/demo-notice`、`eva/demo-card`、`eva/demo-section`、`eva/demo-cta` 四个演示区块（分别对应 `createBlock` 的四种形态）。文章里如果插过这些区块，删完会变成空区块——区块的值存在文章内容里，不在数据库。

> 删除 demo **不影响框架本体**：`\Eva` 门面（`includes/class-eva.php`）与各容器渲染/保存逻辑（`includes/core`、`includes/admin`、`includes/containers`）与 demo 无依赖关系。

---

## 8. 上线检查清单

- [ ] `wp-config.php` 设 `define('EVA_FW_DEV', false);`（关闭 live-reload 与 mtime 缓存击穿，资源版本回落到 `EVA_FW_VERSION`）。
- [ ] demo 已按 §7 删除。
- [ ] 已迁模块的字段类型，前端组件与后端 sanitize **都已具备**（否则该字段不可用 / 会丢值）。
- [ ] 评估 CDN 依赖（Vue3 / Remixicon 走 jsDelivr）：内网或强稳定性场景建议本地化到主题 `Assets/`。
- [ ] 迁移期 CSF 与 Eva 共存时，确认同一设置不会被两套框架重复注册到同一菜单 / 同一 option 键。
- [ ] 面板皮肤保持默认（只接管 Eva 自己的面板）：确认没有误开 `\Eva::skinAdminBoxes(true)`，否则官方面板也会跟着改样。

---

## 9. 最小可用示例（主题内）

```php
add_action('init', function () {
    if (! class_exists('\Eva')) { return; } // 容错：Eva 未加载则跳过

    \Eva::createOptions('Leantasy', array(
        'menu_title' => 'LF主题设置',
        'menu_slug'  => 'Leantasy',
        'location'   => 'admin_bar',
        'standalone' => true,
    ));

    \Eva::createSection('Leantasy', array(
        'id'     => 'general',
        'title'  => '常规设置',
        'fields' => array(
            array('id' => 'site_slogan', 'type' => 'text', 'title' => '站点标语', 'default' => ''),
        ),
    ));
});

// 模板里取值（与 CSF 时代写法一致）
echo esc_html(\LF\Modules\Settings\Main::get_option('site_slogan'));
```

---

## 10. CSF 兼容层（`csf_compat`）

目标：主题里按 CSF 写法注册的分区**原样搬过来就能用**，而且**库里的存值结构不变**，`_LF_Settings()` 等读取层不用改。
实现在 `includes/core/class-eva-csf-compat.php`；完整可运行的例子是 `includes/demo-csf-compat.php`（后台顶部工具栏 →「CSF 兼容演示」，最后一个分区会打印库里的原始值）。

### 怎么开

```php
\Eva::createOptions('Leantasy', array(
    'menu_title' => 'LF主题设置',
    'menu_slug'  => 'Leantasy',
    'csf_compat' => true,   // 整个容器按 CSF 的写法与存值格式处理
    // 不写 menu：侧栏由各分区的 title / icon / parent 自动生成（同 CSF）
));

// 也可以只给某一个分区开，迁一个开一个：
\Eva::createSection('Leantasy', array('title' => '页脚', 'csf_compat' => true, 'fields' => array(/* … */)));
```

`createMetabox / createTaxonomyOptions / createNavMenuOptions / …` 同样认 `csf_compat`。

### 始终生效的映射（只在出现 CSF 独有写法时触发，不影响 Eva 原生写法）

| CSF 写法 | 在 Eva 里 |
| --- | --- |
| `heading` / `subheading` / `submessage` / `notice` | 新增的纯展示字段（`Fields/Notice`），不保存 |
| `fieldset` | Eva 的 `group`（固定对象），存值结构相同 |
| `tabbed` | 新增字段（`Fields/Tabbed` + `Libs/Tabs`），值平铺存储，同 CSF |
| `palette` | 新增字段（`Fields/Palette`），存选中的键 |
| `accordion` 的 `accordions` 键 | 映射到 `sections`，并按 CSF 的**平铺**结构读写 |
| `sorter` 不写 `options`、选项放在 `default` | 从 `default` 取选项，库里存「键 => 标题」 |
| `image_select` 选项里的 `text` | 当作卡片标题（`label`） |
| `'options' => 'categories'` 等字符串数据源 + `query_args` | 后端查库展开；分类按层级缩进。支持 pages / posts / categories / tags / menus / users / sidebars / roles / post_types / locations / 函数名 |
| `content`、`callback` 嵌在容器字段里 | 和顶层一样处理（以前只处理分区第一层） |
| `backup` | `theme_backup` |
| 分区不写 `id`、`parent` 子分区、`priority` 排序 | 自动补 id；没注册 `menu` 时生成一、二级侧栏 |
| 先 `createSection` 后 `createOptions` | 分区先排队，容器出现后挂上（CSF 对顺序没有要求） |
| 导航菜单字段的 `'menu' => '1'` / `'1,3'` | 只在对应层级的菜单项里渲染和保存 |
| `menu_capability` | 等同 `capability` |

### 只在 `csf_compat` 下生效的映射（同名但语义相反，必须显式打开）

| 类型 | CSF 的含义 | 处理 |
| --- | --- | --- |
| `group` | 可增删的行（值为数组） | 渲染成 `repeater`；`button_title` / `max` / `min` 原样可用，`accordion_title_prefix` → `item_title`，`accordion_title_auto` / `accordion_title_by` → `title_field` |
| `color_group` | 一组命名颜色，值为「键 => 颜色」 | 渲染成新增的 `color_set`（`Fields/ColorSet`） |
| `gallery` | 附件 ID 的逗号串 `"12,15,18"` | 读出时转数组给组件，保存时转回逗号串 |

> Eva 自己的 `group`（固定对象）在兼容容器里请写成 `fieldset`。

### 钩子（把 `csf_` 换成 `eva_`）

| CSF | Eva | 类型 |
| --- | --- | --- |
| `csf_{id}_args` | `eva_{id}_args` | filter：容器配置 |
| `csf_{id}_sections` | `eva_{id}_sections` | filter：分区数组（回调里新增的分区也可以用 CSF 写法） |
| `csf_{id}_save` | `eva_{id}_save` | filter：即将写库的值 |
| `csf_{id}_save_before` / `_saved` / `_save_after` | `eva_{id}_save_before` / `_saved` / `_save_after` | action |
| `csf_options_before` / `csf_options_after` | `eva_options_before` / `eva_options_after` | action，参数 `($id, $opt)`；回调里 `echo` 的 HTML 显示在表单上方 / 下方 |
| `csf_init` | `eva_loaded` | action：框架就绪 |

图标：Eva 的图标选择器原生支持 Remix 全量和页面里的 iconfont symbol（`'library' => 'svg'`），Lentasy 用 `csf_field_icon_add_icons` 注入的两套图标都已覆盖；分区 `icon` 写 `icon-xxx` 会按 `#icon-xxx` symbol 渲染，Font Awesome 类名会换成默认图标（外壳不加载 FA 字体）。

### 还不兼容的（主题目前没用到）

`map` 字段；`typography` / `background` / `border` / `spacing` / `dimensions` / `link_color` / `date` 的 CSF 参数名与存值结构（Eva 有自己的一套）；前台自动加载 Google 字体（`enqueue_webfont`）；设置页的 `show_in_network` / `database` / `show_in_customizer` / `contextual_help`；metabox 的 `page_templates` / `post_formats` / `exclude_post_types`；定制器的 `transport` / `panel`；短代码的经典编辑器按钮与 Gutenberg 区块；通用的自定义图标库过滤器。

### demo 删除

`includes/demo-csf-compat.php` 与 §7 的 demo 一样只是演示：删文件 + 删 `eva-framework.php` 末尾对应的 `require`；它的数据在 `wp_options` 的 `eva_csf_demo` 与 `eva_csf_demo_saved_at`。
